# Global Codex Instructions

- For third-party library, framework, package, SDK, CLI, or API documentation, prefer the Context7 MCP server first.
- Use Context7 especially when the answer depends on version-specific behavior, setup steps, configuration keys, or example code.
- If Context7 does not cover the topic well enough, fall back to official vendor documentation or web search.
- Do not use Context7 for facts that can be verified directly from the local workspace.
